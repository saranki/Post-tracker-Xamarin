<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EnterpriseApp.PostPage">
    
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="PostListView" RowHeight="70" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.View>
                                <Grid Margin="8">
                                    <Label Grid.Column="0" Grid.Row="0" Text="{Binding Title}" FontAttributes="Bold" FontSize="Medium"/>
                                    <Label Grid.Column="0" Grid.Row="1" Text="{Binding Body}" FontSize="10" LineBreakMode="TailTruncation"/>
                                </Grid>
                            </ViewCell.View>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
